services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: prezzy
      POSTGRES_PASSWORD: prezzy
      POSTGRES_DB: prezzy
    ports:
      - "5433:5432"
    volumes:
      - prezzy-db:/var/lib/postgresql/data

volumes:
  prezzy-db:
